{
  "$schema": "./../../CL_Schematic.json",
  "$comment": "This fails because `ItemsToGive` is present, but that requires Type: `ResourceSink` to also be present.",
  "ItemsToGive": [
    {
      "Amount": 1,
      "Item": "Desc_OreIron"
    }
  ]
}